export class Course {
    idCourse!: number; // Assuming this is the equivalent of numCourse in Spring Boot
    level!: number;
    typeCourse!: string; // Assuming TypeCourse is a string enum in Spring Boot
    support!: string; // Assuming Support is a string enum in Spring Boot
    price!: number;
    timeSlot!: number;
}
